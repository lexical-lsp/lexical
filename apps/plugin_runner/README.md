# Lexical.Plugin.Runner

A Plugin runner app.

This app avoids having plugin runtime dependencies in the `lexcial_plugin` app. That app is now free to define only
data structures, and the runtime plugin stuff can go in here.
